# --- 浏览器跨域（CORS）---
# 本 API **固定最宽松** CORS（代码内写死：任意 Origin 回显、预检头回显、不带 cookie），无需也不读取 CORS_ORIGIN / CORS_CREDENTIALS。

# -----------------------------------------------------------------------------
# 重要：dotenv 只会加载「.env」，不会加载「.env.example」。
# 请在本目录复制一份：copy .env.example .env（或手动新建 .env），再修改变量。
# -----------------------------------------------------------------------------
#
# --- NJS-116（本机 Thin 模式）---
# 若查询报错 password verifier type … not supported in Thin mode：
#   - 在此机安装 Oracle Instant Client，设置 ORACLE_INSTANT_CLIENT_LIB_DIR（Windows 指向含 oci.dll 的目录），重启本服务；或
#   - 只调试前端：不要启动本仓库的 Node API，只运行 pcr-ai-report，页面「服务器地址」填已部署的网关（已启用 Thick），例如内网 http://10.192.130.89:30008
#
# --- 正式环境（Oracle 真实数据）---
# 填写下方 ORACLE_*（及 probeweb 相关变量）；不要将 Dummy 设为开启：
#   YIELD_MONITOR_TRIGGERS_DUMMY=false（或不写；PM2 默认亦为 false）
#   INFCONTROL_LAYER_BINS_DUMMY=false（同上）
#
# --- 本地 `npm run dev`（tsx + src，非 dist/production）---
# 未在 .env 中写上述两项时，loadEnv 会默认设为 true（Excel 样本，不连 Oracle）。
# 若本机要连真库联调：PCR_AI_LOCAL_DUMMY=false，或显式 YIELD_MONITOR_TRIGGERS_DUMMY=false 等。
# PCR_AI_LOCAL_DUMMY=false
#
# Oracle（与 smartfactory-exmatrix-api 相同环境变量名）
# 示例业务账号：jbstar_loader（密码勿提交仓库，用本机 .env）
ORACLE_USER=
ORACLE_PASSWORD=
ORACLE_CONNECT_STRING=

# Oracle probeweb（产量相关路由；推荐 URL 前缀 /api/v3，例如 GET /api/v3/yield-monitor-triggers/v3）；连接串默认同 ORACLE_CONNECT_STRING
# ORACLE_PROBEWEB_USER=probeweb
# ORACLE_PROBEWEB_PASSWORD=
# ORACLE_PROBEWEB_CONNECT_STRING=

# -----------------------------------------------------------------------------
# DPI-1050（Oracle Client 版本过低）
# node-oracledb 6.x 的 Thick 模式需要 **Oracle Instant Client / Client >= 18.1**。
# 若 detail 含 DPI-1050 且当前为 11.2：请安装 **Instant Client 19+**，设置 **ORACLE_INSTANT_CLIENT_LIB_DIR**
# 指向解压目录（含 libclntsh.so / oci.dll），并避免让 **ORACLE_HOME** 仍指向 11g 的 lib 抢先被加载；重启 PM2。
# 内网旧路径 /u01/.../client_11.2 若存在仍会尝试加载（与旧行为一致）。若在新环境必须禁止该回退，设：
# ORACLE_SKIP_LEGACY_CLIENT_11=true
# -----------------------------------------------------------------------------
# 将下方取消注释并改成你服务器上 Instant Client 目录（内含 libclntsh.so / oci.dll），
# 然后重启 PM2（或 Node 进程）。启动时 oracle.ts 会调用 oracledb.initOracleClient({ libDir })。
# 这与 Dummy 开关无关：dummy 仅跳过真实查询，不能替代 Thick。
# -----------------------------------------------------------------------------
# Linux / PM2 / Docker：同上，指向解压后的 Instant Client 目录（内含 libclntsh.so）
# ORACLE_INSTANT_CLIENT_LIB_DIR=/opt/oracle/instantclient_21_16
# 可选：LD_LIBRARY_PATH 也可设为同一目录（按 Oracle 文档）
#
# Windows：指向含 oci.dll 的目录
# ORACLE_INSTANT_CLIENT_LIB_DIR=C:\\oracle\\instantclient_21_16
# 可选：tnsnames.ora / sqlnet.ora 所在目录
# ORACLE_CLIENT_CONFIG_DIR=
# 连接池（可按负载在 .env 中调大 ORACLE_POOL_MAX 等；默认与早期版本一致为 4）
# ORACLE_POOL_MIN=0
# ORACLE_POOL_MAX=4
# ORACLE_POOL_INCREMENT=1
# 从池中取连接的排队超时（毫秒），默认 60000；仅调大不能替代调大 poolMax / 优化慢 SQL
# ORACLE_QUEUE_TIMEOUT=60000
# 产量路由专用 probeweb 池（未设置则与主池共用 ORACLE_POOL_* 数值）
# ORACLE_PROBEWEB_POOL_MIN=
# ORACLE_PROBEWEB_POOL_MAX=
# ORACLE_PROBEWEB_POOL_INCREMENT=
# 单次数据库往返最大耗时（毫秒），0 表示不限制（默认 0）
# ORACLE_CALL_TIMEOUT_MS=
# 借连接执行整段回调超过该毫秒数时打 warn；默认关闭，需排查慢 SQL 时再设正数
# ORACLE_SLOW_QUERY_LOG_MS=

# GET /api/v3/table-rows（与 /api/v1/table-rows 同路由）未传 table= 时使用的默认表（TABLE 或 SCHEMA.TABLE）
# ORACLE_DEFAULT_TABLE=MY_TABLE

# **v4** 层控与产量聚合在 **Oracle** 路径下于 Node 内聚合时，单次允许拉取的最大匹配行数（防 OOM；超出返回 422 QUERY_TOO_LARGE）。**v3** Oracle 聚合仍在库内完成，不使用本上限。
# 正整数，默认 200000，上限 5000000。
# MEMORY_AGG_ORACLE_MAX_ROWS=200000

PORT=3000

# --- AI Agent（报表聊天页 POST /api/v4/agent/chat）---
# 是否对所有用户启用 AI Agent 标签页；默认 true。设为 false 后所有人的导航栏均隐藏 AI Agent。
# 改后需 pm2 reload 才能生效（通过 GET /health 的 agentEnabled 字段下发到前端）。
# AGENT_ENABLED=true
#
# 正式环境必须设置 AGENT_API_KEY，否则未在前端设置 API Key 的用户会看到 400 CONFIG_ERROR。
# 前端如在 Settings 中填了 API Key，则以前端设置为准（优先级：请求体 > AGENT_API_KEY > SILICONFLOW_API_KEY）。
AGENT_API_KEY=
# AGENT_API_BASE=https://api.siliconflow.cn/v1
# AGENT_MODEL=deepseek-ai/DeepSeek-V4-Flash
# AGENT_SUB_MODEL=deepseek-ai/DeepSeek-V4-Flash
# 生产推荐 Flash（真库 handoff/Pass B-C 验证）；勿用 DeepSeek-V4-Pro 作默认（易 250s 超时、分类器抢路由）
#
# 仅允许两个模型族（agentConfig.ts 的 isAllowedAgentModel，按归一化子串模糊匹配，
# 不要求与某个供应商的完整 ID 字符串精确一致）：
#   1) DeepSeek-V4-Flash — 如 deepseek-ai/DeepSeek-V4-Flash
#   2) MiniMax-M2.5      — 如 Pro/MiniMaxAI/MiniMax-M2.5（硅基流动）
# 供应商（AGENT_API_BASE）可换成其它 OpenAI 兼容平台（如七牛云）而无需改代码，
# 只要 AGENT_MODEL 的字符串里仍包含 "DeepSeek-V4-Flash" 或 "MiniMax-M2.5"
# （大小写、分隔符不敏感）。填其它不认识的模型名会静默回退到 DeepSeek-V4-Flash。
# 单次对话 Agent 连续调工具的最大轮数（1–20）；前端 Settings 可覆盖，未传时可用此默认值
# AGENT_MAX_ROUNDS=5
# 单次工具结果 JSON 最大字符数（6000–30000）；前端 Settings 可覆盖，默认 12000
# AGENT_TOOL_RESULT_MAX_CHARS=12000
# AI Agent 流式请求 idle 超时（毫秒），默认 150000；有 SSE 字节则重置计时
# AGENT_STREAM_TIMEOUT_MS=150000
# 数据脱敏审计证据（JSONL，含 ISO 时间戳与替换次数）
# ⚠️ outbound_mask 记录含 outboundMessages（发给 LLM 的完整 masked 内容）与
# realDeviceValues（对应真实 device 值列表）——按需求追加，此文件因此含真实
# device 标识符，视为敏感文件（限制读取权限、不外发）。
# 默认写入 logs/agent-data-masking-audit.jsonl；设 false 则只打控制台、不写文件
# AGENT_DATA_MASKING_AUDIT=true
# AGENT_DATA_MASKING_AUDIT_PATH=

# --- 硅基流动（旧直连路由 GET /siliconflow/chat，可选）---
# GET /api/v1/siliconflow/chat?message=你好 等（同路由亦挂在 /api/v3、/api/v4）
# 文档：https://docs.siliconflow.cn/cn/api-reference/chat-completions/chat-completions
# API Key 已硬编码在 src/lib/siliconflowChat.ts（SILICONFLOW_API_KEY 常量）；无需在 .env 配置密钥。
# 可选：若将来改回 env 覆盖，可取消下行注释（当前代码不读取本变量）
# SILICONFLOW_API_KEY=
# 默认 Pro/MiniMaxAI/MiniMax-M2.5；可改为控制台中的其它模型 id
# SILICONFLOW_MODEL=Pro/MiniMaxAI/MiniMax-M2.5
# 默认 https://api.siliconflow.cn/v1
# SILICONFLOW_API_BASE=https://api.siliconflow.cn/v1
# 出站调用硅基流动超时（毫秒），默认 60000，范围 5000–120000
# SILICONFLOW_FETCH_TIMEOUT_MS=60000
# 企业 HTTPS 解密（自签证书链）时：设为 true 后仅对硅基流动出站跳过 TLS 校验（有 MITM 风险）。若未设置，代码内默认与 true 相同（方便无法改服务器 .env 的部署）；要强制校验请设 SILICONFLOW_TLS_STRICT=true
# SILICONFLOW_TLS_INSECURE=true
# 与上两者相反：强制校验 TLS（证书链不通过则失败）
# SILICONFLOW_TLS_STRICT=true
# 更稳妥：Node 额外信任企业根 CA PEM 文件路径（与 TLS_INSECURE 二选一优先用本项）
# NODE_EXTRA_CA_CERTS=/path/to/corp-root.pem

# 测试 / 无 Oracle probeweb：v3 列表 GET /api/v3/yield-monitor-triggers/v3（同路由亦挂载在 /api/v1/...）可走内存样本，不连库（传参与 JSON 形状与真实接口一致）
# 设为 1 / true / yes；或 NODE_ENV=test 时也会启用 dummy（无需再设本变量）
# 注意：`npm run build` 后的 dist 进程，或 NODE_ENV=production 时，Dummy 恒关（走 Oracle），本变量无效。
# YIELD_MONITOR_TRIGGERS_DUMMY=true

# 测试 / 无 Oracle 主库：v3 层控 GET /api/v3/infcontrol-layer-bins/v3 与 …/v3/aggregate（同路由亦挂载在 /api/v1/...）可走内存样本，不连库（传参与 JSON 形状与真实接口一致）
# PM2 下 NODE_ENV 常为 production，若未配置 Oracle，显式打开本变量可跳过主库查询（联调用）
# 设为 1 / true / yes；或 NODE_ENV=test 时也会启用 dummy
# 注意：`npm run build` 后的 dist 进程，或 NODE_ENV=production 时，Dummy 恒关（走 Oracle），本变量无效。
# INFCONTROL_LAYER_BINS_DUMMY=true

# --- GET /api/v1/inf-analysis/site-bin-bylot（Perl output_site_bin_bylot.pl）---
# 用法示例（curl / Dummy）：见 docs/SITE_BIN_BY_LOT_API.md
# 按 wafer 测试 pass（可多个 passId）读 INF：各 bin 测试结果由 probe card 哪个 DUT 测得（非 Oracle JB）。
# API 主机须已安装 Perl 及 INFAnalysis 模块（与脚本 shebang / use lib 一致）。
# PERL_BIN=/usr/local/bin/perl
# PERL_SCRIPT_TIMEOUT_MS=120000
# 可选：限制 infPath 必须位于该目录下（绝对路径比较）
# INF_PATH_ALLOWED_ROOT=/data/inf
# buildInfPath() 用来拼接 device + lot + slot 的存储根目录；默认 /data/INF
# INF_STORAGE_ROOT=/data/INF
# 联调 / 无 Perl：固定样本 docs/site-bin-bylot-dummy-r_1-1.passes.json
# SITE_BIN_BY_LOT_DUMMY=true 时仅 canonical 路径（见 SITE_BIN_BY_LOT_API.md）返回样本
# INFCONTROL_LAYER_BINS_DUMMY=true（本地 npm run dev 默认）时，报表 buildInfPath 任意路径也返回同一样本
# SITE_BIN_BY_LOT_DUMMY_RELAX_PATH=true 可单独放宽路径（不必开 JB Dummy）
# Lot 聚合（device+lot+probeCardType）：JB 命中且磁盘可读的 wafer 上限，默认 25
# SITE_BIN_BY_LOT_MAX_WAFERS=25
# Device 聚合（device+probeCardType，无 lot）：跨 lot wafer 上限，默认 100
# SITE_BIN_BY_LOT_MAX_WAFERS_DEVICE=100
# INF 文件丢失时从 Oracle 回退（INFLAYERMAP.BINCODELAST + INFLAYERBINLIST.TESTSITELAST）；默认开启；Dummy 模式下自动关闭
# SITE_BIN_ORACLE_FALLBACK=false

# AI Agent 会话日志（每次对话请求写一个 .md 文件，文件名为时间戳，方便评估）
# 默认目录：pcr-ai-api/session-logs/（相对于项目根目录）
# SESSION_LOG_DIR=./session-logs

# JB 路由 LLM 兜底分类器(模糊问句走便宜模型判意图);默认关,关闭=纯正则路由
JB_LLM_INTENT_CLASSIFIER=false

# 阶段三:决策驱动确定性派发(默认 off)。on 时对高置信跨实体 mode
# (bin_card_attribution/lot_yield_ranking/card_yield_compare)在 LLM 前服务端直发查询。
# 翻 on 前须先在真库验证 LLM 误分类率达标(见 docs/superpowers/specs/2026-06-29-jb-deterministic-dispatch-design.md)。
JB_DETERMINISTIC_DISPATCH=false

# --- Vero Path B 试点（仅「探针卡×机台组合」PRE_LLM 直通路）---
# 对齐 vero-agent-demo Path B：POST /api/simple-agent/invoke 做参数抽取 + 解读；
# 聚合仍走本机 aggregate_probe_card_tester_performance（确定性四表）。其它 Agent 功能不动。
# 需同时设置 WCHAT_ACCESS_TOKEN；未开 flag 或无 token → 仍走 SiliconFlow 解读。
# AGENT_PROBE_CARD_VERO_PILOT=true
# WCHAT_ACCESS_TOKEN=
# VERO_BASE_URL=https://verostudio.sw.nxp.com
# 企业 HTTPS 解密：默认跳过 Vero TLS 校验（与 SiliconFlow 类似）；强制校验设 VERO_TLS_STRICT=true
# VERO_TLS_INSECURE=true
# VERO_TLS_STRICT=true
# 亦可全局：NODE_TLS_REJECT_UNAUTHORIZED=0（影响整进程，不推荐生产）

# --- Vero 驱动的通用 Agent 循环内核（子项目 A）---
# 2026-07-22 起默认开启（不设此变量即视为 true）：AI Agent 的整个 ReAct 循环
# （不仅探针卡组合）改用 Vero 驱动，不再调用 SiliconFlow/DeepSeek/MiniMax。
# 仍需 WCHAT_ACCESS_TOKEN 才真正生效（与上面的 Path B 共用同一个 token）；
# 没配 token 的环境会自动回退 SiliconFlow，不受此默认值影响。
# 需要临时回退 SiliconFlow：显式设 AGENT_VERO_GENERIC_LOOP=false
# 详见 docs/superpowers/specs/2026-07-21-vero-generic-agent-loop-design.md
# AGENT_VERO_GENERIC_LOOP=false
