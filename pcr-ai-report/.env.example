# API 基地址 / 开发代理（可忽略本文件，改用已提交的 `.env.development` / `.env.production`）。
#
# `npm run dev`：不要在本机 `.env` 里设置 `VITE_API_BASE_URL=http://10.x...`，否则浏览器仍从 localhost **直连内网**，易被 Chrome 拦截。应让「服务器地址」留空，由 Vite 代理（见 `.env.development`）。
# `npm run build`：生产静态资源仍用内网或网关完整 URL，见 `.env.production` 的 `VITE_API_BASE_URL`。
# VITE_API_BASE_URL=http://10.192.130.89:30008
