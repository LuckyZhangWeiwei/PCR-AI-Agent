# API 基地址（与仓库内 `.env.development` / `.env.production` 一致时可忽略本文件）。
#
# - `npm run dev`：优先读取 `.env.development`（已提交，指向正式网关）。
# - `npm run build`：读取 `.env.production`。
# - 也可复制本文件为 `.env` 做本机覆盖（勿提交机密）。
VITE_API_BASE_URL=http://10.192.130.89:30008
