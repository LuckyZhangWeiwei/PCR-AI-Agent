# `npm run dev` 时加载。勿让浏览器从 localhost 直连内网 10.x API（Chrome Private Network Access 会拦截）。
# 页面请求发往当前 dev 源（如 http://localhost:5174），由 Vite 代理到下面这个地址。
#
# 若浏览器里 /health、/api 出现 502：说明「本机 Node」连不到该地址（未连公司网/VPN、或网关未启动）。
# 在本机启动 pcr-ai-api 时改为：
#   VITE_DEV_PROXY_TARGET=http://127.0.0.1:30008
# 改完后务必重启 npm run dev。
VITE_DEV_API_VIA_PROXY=true
VITE_DEV_PROXY_TARGET=http://10.192.130.89:30008
# 开发默认「同页相对路径」；若填完整 URL 则直连该地址（一般不推荐）
# VITE_API_BASE_URL=
